assignments:
  test-stub:
    image: "bytegrader-test-stub:latest"
    description: "Simple test grader that returns constant results"
    enabled: true
    timeout_minutes: 5
    resources:
      memory_mb: 256
      cpu_limit: 0.25
      pids_limit: 50
